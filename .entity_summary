Legend: `!` after a field name = required (from the app's base view) —
a step form that writes such a field must not submit it empty.
A lookup field lists its keys after the type (`[lookup/select: a|b|c]`);
these are the ONLY valid write values — quote them into the brief verbatim,
the matching labels come from LOOKUP_OPTIONS at runtime.
`⚙` lists the service methods for that entity, spelled exactly.
`✚` lists the entity's Enriched* fields and its enrich() signature,
spelled exactly — quote both verbatim; an invented enriched field or
a missing map is a guaranteed type error.

Modules — copy these paths verbatim into a subagent brief:
  @/types/app                            entity types, APP_IDS, LOOKUP_OPTIONS
  @/types/enriched                       EnrichedSchichtplan — the COMPLETE list, no other Enriched* type exists
  @/lib/enrich                           the enrich functions — exact fields and signatures on the `✚` line of each entity below; every listed map is REQUIRED, and no other enriched field exists
  @/services/livingAppsService           LivingAppsService, createRecordUrl, extractRecordId
  @/hooks/useDashboardData               useDashboardData — the RAW arrays (one per entity, camelCase), their setters, loading, error, fetchAll + mitarbeiterMap, schichtenMap; it carries NO enriched arrays — build those with the `✚` call below
  @/lib/formatters                       lookupKey, formatDate
  @/components/blocks/IntentWizardShell  IntentWizardShell
  @/components/blocks/EntitySelectStep   EntitySelectStep
  @/components/blocks/StatusBadge        StatusBadge, getStatusColor
  @/components/blocks/BudgetTracker      BudgetTracker
  @/components/blocks/AvailabilityRangePicker  AvailabilityRangePicker, rangeIsFree, isNightBlocked
There is NO `@/config/appIds`, no `@/config/lookupOptions` and no
`@/lib/urlUtils` — APP_IDS and LOOKUP_OPTIONS come from `@/types/app`,
createRecordUrl from the service. Note the lowercase `l` in the service path.

- Mitarbeiter (mitarbeiter): vorname![string/text], nachname![string/text], email[string/email], telefon[string/tel], abteilung[string/text], position[string/text], beschaeftigungsart[lookup/select: vollzeit|teilzeit|minijob|aushilfe], notizen[string/textarea]
  ⚙ getMitarbeiter() · getMitarbeiterEntry(id) · createMitarbeiterEntry(fields) · updateMitarbeiterEntry(id, fields) · deleteMitarbeiterEntry(id)
- Schichten (schichten): schicht_name![string/text], kuerzel[string/text], startzeit![string/text], endzeit![string/text], schicht_beschreibung[string/textarea], schicht_typ[lookup/select: fruehschicht|spaetschicht|nachtschicht|tagschicht|sonderschicht]
  ⚙ getSchichten() · getSchichtenEntry(id) · createSchichtenEntry(fields) · updateSchichtenEntry(id, fields) · deleteSchichtenEntry(id)
- Schichtplan (schichtplan): datum![date/date], mitarbeiter![applookup/select], schicht![applookup/select], arbeitsbereich[string/text], anmerkungen[string/textarea]
  ⚙ getSchichtplan() · getSchichtplanEntry(id) · createSchichtplanEntry(fields) · updateSchichtplanEntry(id, fields) · deleteSchichtplanEntry(id)
  ✚ EnrichedSchichtplan adds ONLY mitarbeiterName, schichtName — via enrichSchichtplan(schichtplan, { mitarbeiterMap, schichtenMap }), all maps required
  → mitarbeiter links to app_id 6a8ea04e7c2210550919382e
  → schicht links to app_id 6a8ea052f67a0c6efea828fa